# Database credentials (sensitive — do not commit)
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=

# Strapi secrets — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_KEYS=
API_TOKEN_SALT=
ADMIN_JWT_SECRET=
TRANSFER_TOKEN_SALT=
JWT_SECRET=
ENCRYPTION_KEY=
